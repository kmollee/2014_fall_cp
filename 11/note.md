# week 11 note

# commmand

`git clone repository`

`git add`

`git commit`

`git push`

`git status`

`git pull repository`

## remote

```
git remote -v
git remote add remote_name remote_url
git set-url remote_name new_url
```
